.next
.open-next
.wrangler
node_modules

# 병행 세션이 만드는 git worktree. 저장소 *안쪽* 경로라(.claude/worktrees/<name>) prettier 가
# 그 체크아웃을 통째로 다시 훑는다 — git 은 nested worktree 를 알아서 무시하지만 파일 훑기는
# 그렇지 않다. 그러면 남이 브랜치를 하나 띄웠다는 이유만으로 로컬 format:check 만 빨개지고
# CI 는 초록이다(CI 엔 그 디렉터리가 없다). 로컬 게이트와 CI 판정이 갈리는 건 이 저장소가
# 가장 피하는 것이다. 2026-07-30 에 실제로 밟았다.
.claude/worktrees
next-env.d.ts
cloudflare-env.d.ts
package-lock.json

# 구 정적 사이트의 frozen 스냅샷(내부 참고 페이지) — 원본 포맷 그대로 보존한다.
docs/reference

# drizzle-kit 이 생성하는 마이그레이션 메타(스냅샷·저널). 산출물이라 손대지 않는다 —
# .sql 은 prettier 파서가 없어 어차피 대상이 아니고, 정본은 스키마와 SQL 이다.
drizzle/meta
